<div
  class="epm-backdrop {{unless this.hasModals "epm-out"}}"
  tabindex="-1"
  role="presentation"
  aria-hidden="true"
></div>

{{#each this.modals._stack as |modal|}}
  <EpmModal @modal={{modal}} />
{{/each}}